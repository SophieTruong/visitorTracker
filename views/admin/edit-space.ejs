<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/space.css">
</head>

<body>
   <%- include('../includes/navigation.ejs') %>
   <!-- enctype="multipart/form-data" -->
    <main>
    <form class="space-form" action="/admin/<% if (editing) { %>edit-space<% } else { %>add-space<% } %>" method="POST" >

            <div class="form-control">
                <label for="spaceName">Space Name</label>
                <input type="text" name="spaceName" id="spaceName" 
                value = "<% if (editing) { %><%= space.spaceName %> <% } %>">
            </div>

            <!-- <div class="form-control">
                <label for="spaceID">Space ID</label>
                <input type="text" name="spaceID" id="spaceID" 
                value = "<% if (editing) { %><%= space.spaceID %> <% } %>"
                required>
            </div> -->

            <div class="form-control">
                <label for="imageUrl">Image URL</label>
                <input type="text" name="imageUrl" id="imageUrl"
                value = "<% if (editing) { %><%= space.imageUrl %> <% } %>">
            </div>
            <!-- <div class = "form-control">
                <label for="image">Image</label>
                <input type = "file" name = "image" id ="image">
            </div> -->
            <% if (editing) {%> 
                <input type = "hidden" value = "<%=space._id%>" name = "spcID">
            <% } %> 

            <button class="btn" type="submit">
                <%if (editing){ %>Update info<% } else { %>Add Space<% } %> 
            </button>
            
        </form>
    </main>
<%- include('../includes/end.ejs') %>